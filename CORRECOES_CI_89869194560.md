# Correções CI — run 89869194560

## Resultado do log

O job API já passava em:

- Compile Python
- Ruff
- MyPy

A suíte unitária terminou com **2 failures / 165 passes / 16 deselected**.

### 1. Compatibilidade de `validate_pair()`

Falha:

`tests/test_html_template_contract.py::test_html_pair_requires_same_template_key`

A versão 2.3.1 ampliou o contrato para LANDING + BOOKING + LOGIN e passou a emitir:

`HTML_TEMPLATE_FAMILY_KEY_MISMATCH`

Por compatibilidade com consumidores 2.3.0, `validate_pair()` deve continuar expondo:

`HTML_TEMPLATE_PAIR_KEY_MISMATCH`

Correção aplicada:

- `validate_family()` continua usando o código novo de família;
- `validate_pair()` traduz somente o mismatch de chave para o código legado;
- `ensure_pair()` voltou a usar `validate_pair()` e `HTML_TEMPLATE_PAIR_INVALID`.

Isso mantém a nova arquitetura sem quebrar o contrato público anterior.

### 2. Contrato textual do Workspace AVB

Falha:

`tests/test_visual_builder_integration_contract.py::test_visual_builder_uses_three_first_class_scheduler_pages`

O botão estava com texto:

`Aplicar · Landing`

O contrato de integração exige que a ação seja explicitamente identificada como template:

`Aplicar template · Landing`

Correção aplicada para as três superfícies:

- `Aplicar template · Landing`
- `Aplicar template · Agendamento`
- `Aplicar template · Login`

O comportamento continua sendo por página; `Aplicar família` permanece ausente.

## Validação após correção

- `test_html_pair_requires_same_template_key`: PASS
- `test_visual_builder_uses_three_first_class_scheduler_pages`: PASS
- ARGWS Visual Builder 2.3.1: 60/60 testes PASS
- `python -m compileall -q app tests`: PASS
- contrato estático do Workspace 2.3.1: PASS

A suíte `pytest` completa não foi reproduzida neste runtime porque o pacote `structlog` não está instalado e o ambiente está sem acesso de rede. O próprio GitHub Actions do run analisado já confirmou Ruff e MyPy verdes; os únicos dois failures reportados foram corrigidos acima.
