# UI legada preservada

Estes arquivos foram retirados da árvore ativa do `apps/web/src` porque representavam implementações antigas da Agenda que já foram substituídas por `TenantAgendaCenter.vue` e pelo Operador global da Agenda.

Eles permanecem aqui apenas como referência histórica e **não participam do build nem da navegação**.
