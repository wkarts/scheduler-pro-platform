# Correções a partir dos logs CI 89862302147 e 89862302062

Data: 2026-08-28

## 1. API / Ruff

O job API parava em `ruff check app tests` com cinco erros:

- `landing_pages.py`: três ocorrências E701 por `try`, `except` e `if` compactados na mesma linha;
- `public.py`: duas ocorrências F821 porque `sqlalchemy.text` era utilizado sem import.

Correções:

- `template_family_surface()` reformatado em blocos Python normais;
- `from sqlalchemy import text` adicionado em `public.py`.

## 2. Integration / readiness

A migração `platform_0012_login_surface` era aplicada corretamente, mas três pontos ainda esperavam o head anterior `platform_0011_global_templates`:

- `app/api/v1/routes/health.py`;
- `tests/test_foundation_integration.py`;
- `tests/test_health_readiness_contract.py`.

Isso fazia `/api/v1/health/ready` responder 503 com:

`migration:platform_0012_login_surface`

mesmo com o banco corretamente migrado.

Correção:

`PLATFORM_MIGRATION_HEAD = "platform_0012_login_surface"`

foi aplicado no readiness e nos contratos/testes correspondentes.

## 3. O que os logs já confirmavam como saudável

- Web/typecheck/build: aprovado;
- Docker: todas as imagens construídas;
- Integration: 14/16 aprovados; os 2 failures eram somente o head de migração 0011 vs 0012.

## 4. Validações locais após correção

- Python `compileall`: OK;
- regressão textual dos cinco erros Ruff: corrigida;
- constantes de migration head: alinhadas em 0012;
- ARGWS Visual Builder 2.3.1: 60/60 testes aprovados;
- templates oficiais: 8/8 válidos, cada um com LANDING + BOOKING + LOGIN;
- dialogs nativos restantes encontrados são somente a API oficial de instalação PWA (`prompt.prompt()`), não `window.prompt()` da aplicação.

A suíte Python completa não foi executada localmente porque este runtime não possui as dependências da API instaladas e o acesso externo para `pip` está indisponível. Os logs do GitHub Actions mostram que essas dependências são instaladas normalmente no CI.
