# Scheduler Pro Desktop

Aplicativo gerencial Tauri 2 para Windows, macOS e Linux.

Este app não implementa o SaaS dentro do desktop. Ele consome a API FastAPI do Scheduler Pro e reaproveita o padrão visual Tailwind/Vue.

```bash
npm install
npm run tauri:dev
```
