# Scheduler Pro / RabbitMQ 4 compatibility
# Preferimos desativar mingle/gossip/remote-control no Celery, mas este fallback
# permite subir stacks existentes que ainda usem filas temporárias não exclusivas.
deprecated_features.permit.transient_nonexcl_queues = true
